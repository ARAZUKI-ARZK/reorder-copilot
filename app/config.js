/**
 * App-wide settings & Feature Flags
 *
 * EARLY_ACCESS_MODE:
 *   true  → All features unlocked for free (current)
 *   false → FREE_SKU_LIMIT enforced, paid plan required to exceed
 */
export const EARLY_ACCESS_MODE = true;

export const FREE_SKU_LIMIT = 50;
